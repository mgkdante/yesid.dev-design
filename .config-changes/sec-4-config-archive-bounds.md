---
bump: patch
---

Bound config archive and checksum reads, compare deterministic tagged bytes before parsing, and enforce compressed, expanded, member, payload, entry, and command-execution limits.
