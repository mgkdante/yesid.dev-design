{
	"name": "@yesid/gates",
	"version": "0.9.0",
	"private": true,
	"type": "module",
	"description": "Pure, parameterized brand-quality gate engines. Product policy stays consumer-owned; this package is test-runner-agnostic and depends only on Node filesystem APIs.",
	"sideEffects": ["**/*.css"],
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"check": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.5.0",
		"vitest": "^4.1.0"
	}
}
