{
	"name": "@yesid/i18n-core",
	"version": "0.13.1",
	"private": true,
	"type": "module",
	"description": "Pure, consumer-configured locale-routing mechanics with app-owned locale values, exemption policy, and runtime guards.",
	"sideEffects": false,
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"check": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.5.0",
		"vitest": "^4.1.0"
	}
}
